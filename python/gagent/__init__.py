"""gagent - Unified CLI for the six-tool agent stack.

This Python package is a thin launcher that ships a bundled JavaScript
build of the gagent CLI and runs it through the user's local Node.js
runtime (Node >= 18 required).
"""

__version__ = "0.5.0"
